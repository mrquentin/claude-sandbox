{
  "// docs": "claude-sandbox user configuration — copy to ~/.config/claude-sandbox/config.json",

  "tools": {
    "// paths_doc": "Extra directories to add to PATH inside the sandbox",
    "paths": [
      "/opt/custom-tool/bin"
    ],

    "// ro_binds_doc": "Extra directories to bind-mount read-only (for tool dependencies)",
    "ro_binds": [
      "/opt/custom-tool"
    ],

    "// rw_binds_doc": "Extra directories to bind-mount read-write",
    "rw_binds": []
  },

  "seccomp": {
    "// block_doc": "Additional syscalls to block (added to the default list). Run 'claude-sandbox --list-syscalls' to see available names.",
    "block": [
      "io_uring_setup",
      "io_uring_enter",
      "io_uring_register"
    ]
  }
}
